import React, { useState } from 'react'
import axios from 'axios'

export default function Modal({ image, name, house, species, dateOfBirth, gender, patronus, wand: [wood, core, legend], ancestry, actor }) {
    return (
        <section>
            <div>
                {nome &&
                {resposta.image}
                {resposta.name}
                {resposta.house}
                {resposta.species}
                {resposta.dateOfBirth}
                {resposta.gender}
                {resposta.patronus}
                {resposta.wand}
                {resposta.wand.wood}
                {resposta.Wand.core}
                {resposta.wand.legend}
                {resposta.ancestry}
                {resposta.actor}
            </div>
                }
        </section>
    )
}